/**
 * Hooks Index
 * 
 * Re-exports all custom hooks from the hooks directory.
 * Import hooks from this file for a cleaner import path.
 * 
 * Example:
 *   import { useSampleData } from './hooks';
 */

export * from './useFetchData';
